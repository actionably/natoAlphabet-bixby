endpoints {
   action-endpoints {
     action-endpoint (NatoSpell) {
       accepted-inputs (textToSpell, $vivContext)
       local-endpoint ("NatoSpell.js")
     }
    action-endpoint (NatoAlphabet) {
      accepted-inputs ()
      local-endpoint ("NatoAlphabet.js")
    }
   }
}